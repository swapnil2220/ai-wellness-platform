# Gemini API Key (Required for live AI generation; fallback mock mode is active when empty)
GEMINI_API_KEY=your_gemini_api_key_here

# App Environment Configuration
APP_ENV=development
DATABASE_URL=sqlite:///./database/wellness.db
PORT=8501
HOST=0.0.0.0
